version: '3'
services:

  qgis:
    build:
      context: ..
      dockerfile: ./.docker/qps-qt5.dockerfile
      args:
        QGIS_TEST_VERSION: ${QGIS_TEST_VERSION}
    tty: true
    volumes:
      - ${GITHUB_WORKSPACE}:/usr/src
